# 多智能体区域覆盖任务 - Demo讲稿

## 一、问题设置

### 1.1 核心任务目标

**多智能体协同区域覆盖巡逻**：在矩形区域内协调多个异构机器人，使整个区域的探测覆盖保持"新鲜"状态。

### 1.2 核心挑战

| 挑战 | 描述 |
|-----|------|
| **异构性** | Agent具有不同的速度-探测范围权衡 |
| **持续覆盖** | 不仅要覆盖，还要定期重访已覆盖区域 |
| **协调分工** | 避免多个Agent重复覆盖同一区域 |

---

## 二、场景假设

### 2.1 智能体特征

#### 2.1.1 兵力编成（文档口径）

我方现有 YJ 监视兵力包含 **无人艇 4 艘、无人机 6 架**：

- **无人艇 I 型**：2 艘  
- **无人艇 II 型**：2 艘  
- **无人机 I 型**：4 架  
- **无人机 II 型**：2 架  

#### 2.1.2 平台与载荷能力参数（文档口径，摘要）

| 平台 | 数量 | 航速范围 | 续航能力 | 对海探测/雷达探测范围（单一工作模式） | 光电识别距离 |
|---|---:|---|---:|---|---|
| 无人艇 I 型（参考“天剑系列”） | 2 | 0–30 节 | 20 小时 | 2–25 km | - |
| 无人艇 II 型（参考“虎鲸”） | 2 | 0–35 节 | 115 小时 | 2–25 km | - |
| 无人机 I 型（彩虹-5） | 4 | 45–85 m/s | 40 小时 | 5–150 km | 1–30 km |
| 无人机 II 型（彩虹-4） | 2 | 30–65 m/s | 30 小时 | 2–120 km | 1–20 km |

> 备注：当前 demo 只覆盖“运动 + 覆盖探测”两要素，**未显式建模续航/多工作模式/识别任务**，这些将作为后续扩展项。

#### 2.1.3 Demo 版本的简化建模（与代码实现一致）

当前代码（`scenario_coverage.py`）为了快速跑通训练与可视化，使用 **4 个智能体、2 种类型**来代表“异构平台”：

| 数量 | Demo 类型 | 最大速度（归一化单位） | 探测半径（归一化单位） | 颜色 | 直观定位 |
|:---:|:---:|:---:|:---:|:---:|---|
| 2 | type_A | 1.0 | 0.18 | 蓝色 | 慢速/大范围覆盖 |
| 2 | type_B | 1.5 | 0.12 | 绿色 | 快速/小范围补漏 |

**说明**：这是一种“**代理建模**”（proxy）——用少量类型先验证算法链路与指标（Max gap / Fresh ratio），后续再把类型扩展到“艇/机 I/II”四类并引入续航等约束。

**通讯与可观测性假设**（对应当前代码实现）：每个智能体的观测中包含 3 个**全局标量特征**（整体 `fresh_ratio` / 全局 `max_age` / 回合进度），相当于“中央态势广播给所有平台”。因此当前 Demo **未显式建模通信带宽/延迟/丢包**，也未限制平台间的信息共享。

### 2.2 覆盖区域与网格划分

| 参数 | 值 | 说明 |
|------|-----|------|
| 区域大小 | 1.0×1.0 | 归一化矩形覆盖区域 |
| 网格划分 | 10×10 | 共100个探测单元 |
| 单网格面积 | 0.01 | 占总区域1% |
| 时间离散 | 1步 | 物理仿真 `dt=1.0`；任务层面可将“1步≈1分钟”理解（便于对应“10分钟重访约束”） |
| 重访限制 | 10步 | 网格"过期"阈值（任务层面可理解为≈10分钟） |
| 最大步数 | 200步 | 每回合上限（任务层面可理解为≈200分钟） |

### 2.3 覆盖状态追踪机制

- **last_seen**: 每个网格最后被探测到的步数
- **age = step_count - last_seen**: 网格年龄
- **age ≤ revisit_limit**: 网格"新鲜"
- **age > revisit_limit**: 网格"过期"

---

## 三、模型选择

### 3.1 算法: MAPPO (Multi-Agent Proximal Policy Optimization)

| 组件 | 配置 |
|------|------|
| 算法类型 | 多智能体策略梯度算法 |
| 基础框架 | PPO (Proximal Policy Optimization) |
| 训练范式 | CTDE（集中训练、分散执行） |
| Actor网络 | **共享策略**（参数共享），同一网络控制全部 agents，通过类型特征区分异构能力 |
| Critic网络 | **集中式 critic**（centralised=True），输入拼接所有 agents 的观测用于更稳定的价值评估 |
| 动作空间 | 连续动作（2维），用 `TanhNormal` 采样并映射到动作上下界 |

### 3.2 网络结构

**Actor网络** (12维输入):
- 2维: 归一化位置
- 2维: 归一化速度
- 2维: 类型独热编码
- 2维: 类型特征(速度/探测范围能力)
- 3维: 全局特征(覆盖率/最大年龄/进度)
- 1维: 局部新鲜度(3×3网格均值)

**Critic网络**:
- 输入: 拼接所有Agent的观测向量
- 输出: 状态价值估计

---

## 四、奖励函数设置

### 4.1 三元奖励函数

```
reward = fresh_ratio
         + 0.5 * (fresh_ratio_t - fresh_ratio_{t-1})   # improve：鼓励“持续变好”
         - 0.2 * max_age_norm                          # 惩罚存在“长期没覆盖”的漏洞
```

其中：
- `fresh_ratio` 是所有网格的平滑“新鲜度分数”均值（不是二值达标比例）
- `max_age_norm = max_age / revisit_limit`，并进行截断以避免数值不稳定

### 4.2 核心Trick: 非线性新鲜度惩罚

```python
age_norm = age / revisit_limit      # 归一化
age_curve = clamp(age_norm, 0, 1) ** 3.0  # 三次方曲线
fresh_score = 1 - age_curve
```

**设计意图**: 接近阈值时惩罚急剧增加，强制Agent定期巡逻即将过期的区域。

### 4.3 奖励组件说明

| 组件 | 权重 | 作用 |
|------|------|------|
| fresh_ratio | 1.0 | 激励整体覆盖“新鲜度”提升 |
| improve | 0.5 | 激励持续改进、避免停滞 |
| max_age_norm | -0.2 | 抑制“出现单点长期未覆盖” |

---

## 五、如何看效果（可视化与指标）

- **可视化脚本**：
  - `visualize_coverage_vmas.py`：网格从绿→红渐变（连续显示“新鲜度衰减”）
  - `visualize_coverage_vmas_binary.py`：阈值跳变（≤10步为绿，否则红），更贴近“10分钟硬约束”的直观达标判断
- **视频叠加指标**（PIL 标注在帧上）：
  - `Max gap: X min`：当前全图最长未覆盖时长（按“1步≈1分钟”的任务层面解释）
  - `Fresh ratio: Y%`：阈值下（≤10步）仍“达标”的网格比例
  - `Cell size: W x H`：当前网格单元的物理尺寸（随随机矩形大小变化）

---

## 六、后续改造方向

### 6.1 容易添加的改造 (短期)

#### 改造1: 增加 Agent 数量参数（对齐文档的“资源使用统计/兵力约束”）

**目标**: 探索派出多少Agent可以实现最优覆盖任务

**实现方案**:
```
num_agents: 可配置参数 [2, 4, 6, 8, ...]
- 动态调整观测维度和网络输入
- 添加资源使用惩罚项
```

**参考文档v3**（资源使用统计/兵力约束思路）：
- 状态侧加入：`n_uav_norm = n_uav / 10`，`n_usv_norm = n_usv / 6`
- 奖励侧加入：兵力/能耗惩罚（例如 `R_resource = -κ_res * n_active`）

#### 改造2: 模拟参数与真实装备对齐

**目标**: 将Demo参数与DJ防御场景文档中的装备能力按比例对齐

| Demo参数 | 文档参数（参数文档） | 对齐方式 |
|----------|----------|----------|
| 速度（type_A/type_B） | 无人艇 0–30/35 节；无人机 30–85 m/s | 选定“1仿真单位速度”对应多少 m/s（或节），按比例缩放；必要时分“艇/机”不同动力学 |
| 探测半径（0.12/0.18） | 无人艇探测 2–25 km；无人机雷达探测 2–150 km | 选定“仿真 1.0 长度单位”对应多少 km，保持相对比例；并把艇/机探测上限拉开 |
| 类型与数量（2类4个） | 4类10个（2+2+4+2） | 扩展到“艇/机 I/II”四类型，并把 `n_agents` 做成可配置（含资源惩罚） |
| 时间尺度（dt=1.0） | 重访约束 10 分钟 | 明确“1步≈1分钟”或改为秒级步长+分钟级统计更新 |

#### 改造3（推荐优先做，成本低）: 增加“区域优先级/目标存在”两类区域特征（对齐文档 v3 的区域特征设计）

**动机**：文档 v3 明确提出“覆盖优先级”“目标存在/上次识别时间”等区域特征；这会把 demo 从“均匀涂色”升级为“有重点、有价值的覆盖/识别”。

**最小可落地版本**：
- **状态**：为每个网格增加 `priority∈[0,1]`（静态/随机），可选加 `target_present∈{0,1}`
- **奖励**：把 `fresh_ratio` 改成**优先级加权平均**（优先级高的网格更重要）
  - \(fresh\_ratio = \frac{\sum_i priority_i \cdot fresh\_score_i}{\sum_i priority_i}\)

### 6.2 中期改造方向 (参考文档v3)

| 改造项 | 描述 | 复杂度 |
|--------|------|--------|
| 状态区分 | 添加5种任务状态(巡逻/识别/返航/充电/待命) | 中 |
| 覆盖率历史统计 | 添加过去K步/过去1小时覆盖率作为状态输入 | 低 |
| 约束违反记录 | 记录“超过10分钟未覆盖”的累计次数并惩罚 | 低 |
| 分层动作空间 | 高层调度+低层执行 | 高 |
| 异构Agent扩展 | 区分无人艇/无人机两种大类 | 中 |


---

## 七、总结

本Demo基于**VMAS仿真平台**，使用**MAPPO算法**实现了多异构Agent的区域覆盖任务。通过**非线性新鲜度惩罚**和**三元奖励函数**引导Agent协同工作。

下一步计划:
1. 添加Agent数量可配置参数
2. 将模拟参数与真实装备能力对齐
3. 引入“区域优先级/目标存在”等文档v3关键区域特征，形成“有重点的覆盖/识别”

---

*讲稿生成时间: 2026-02-10*
*基于: `task_design_summary.md` + `DJ防御场景BL任务规划-实施方案_v3.md` + 当前代码实现（`scenario_coverage.py` / `train_coverage_vmas_mappo.py`）*
