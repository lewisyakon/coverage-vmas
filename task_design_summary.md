# VMAS 区域覆盖任务 - 任务与场景设计简析

## 1. 任务概述

### 1.1 任务目标

**多智能体区域覆盖巡逻**：在矩形区域内协调多个异构机器人，使整个区域的探测覆盖保持"新鲜"状态。

**核心挑战**：
- **异构性**：Agent具有不同的速度-探测范围权衡
- **持续覆盖**：不仅要覆盖，还要定期重访已覆盖区域
- **协调分工**：避免多个Agent重复覆盖同一区域

### 1.2 Agent配置

| 数量 | 类型 | 速度 | 探测范围 | 颜色 |
|:----:|:----:|:----:|:--------:|:----:|
| 2 | type_A | 慢(1.0) | 大(0.18) | 蓝色 |
| 2 | type_B | 快(1.5) | 小(0.12) | 绿色 |

---

## 2. 场景设计

### 2.1 环境参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 区域大小 | 1.0×1.0 | 矩形覆盖区域 |
| 网格划分 | 10×10 | 100个探测单元 |
| 重访限制 | 10 | 网格"过期"阈值(步数) |
| 最大步数 | 200 | 每回合上限 |

### 2.2 覆盖状态追踪

- **last_seen**: 每个网格最后被探测到的步数
- **age = step_count - last_seen**: 网格年龄
- **age ≤ revisit_limit**: 网格"新鲜"
- **age > revisit_limit**: 网格"过期"

---

## 3. 核心trick

### 3.1 非线性新鲜度惩罚

```python
age_norm = age / revisit_limit  # 归一化
age_curve = clamp(age_norm, 0, 1) ** 3.0  # 三次方
fresh_score = 1 - age_curve
```

**效果**：接近阈值时惩罚急剧增加，强制Agent定期巡逻。

### 3.2 三元奖励函数

```python
reward = fresh_ratio                    # 基础覆盖奖励
         + 0.5 * improve               # 改进奖励（鼓励持续探索）
         - 0.2 * max_age_norm           # 过期惩罚（惩罚长期不覆盖）
```

### 3.3 异构Agent处理 - 共享Actor + 类型特征

所有Agent共享同一策略网络，通过观测中的特征区分行为：

```python
# 观测向量（12维）
obs = [
    pos_norm,      # 2维 - 位置
    vel_norm,      # 2维 - 速度
    type_one_hot,  # 2维 - 类型编码
    type_feats,    # 2维 - 速度/探测范围能力
    global_feats,  # 3维 - 全局覆盖率/最大年龄/进度
    local_fresh,   # 1维 - 周围3×3网格新鲜度
]
```

### 3.4 局部覆盖提示

Agent观测中包含周围3×3网格的新鲜度均值，帮助发现覆盖空洞：

```python
radius = max(cell_w, cell_h) * 1.5
local_mask = dist <= radius
local_fresh = masked_smooth.mean()
```

### 3.5 随机区域增强（可选）

```python
randomize_area=True
width_range=(0.8, 1.2)
height_range=(0.8, 1.2)
```

增加环境多样性，提高模型泛化能力。

---

## 4. 任务特性总结

| Trick | 作用 |
|-------|------|
| 非线性新鲜度曲线 | 防止Agent忽视即将过期区域 |
| 改进奖励 | 激励持续提高覆盖率 |
| 类型特征观测 | 共享策略处理异构Agent |
| 局部新鲜度提示 | 帮助发现覆盖空洞 |
| 随机区域 | 提高泛化能力 |

---

**核心思想**：通过精细的奖励设计和观测构造，引导异构Agent协同覆盖整个区域，并在覆盖"过期"前主动巡逻。
