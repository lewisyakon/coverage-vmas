# 方案A（MAPPO）与方案B（集中式PPO）任务化设计与 Demo 映射

> 本文按当前讨论口径命名：  
> - **方案A：MAPPO（CTDE，多智能体）**  
> - **方案B：集中式PPO（单中央策略输出全体动作）**

---

## 1. 任务定义（真实任务角度）

目标是在任务时窗内同时满足：
- 区域覆盖重访约束（例如任一点 10 分钟内至少被覆盖一次）
- 重点区域优先覆盖
- 目标识别周期约束（后续阶段）
- 兵力与能耗约束（尽量少出动、低代价）

关键点：决策不仅是“怎么动”，还包括“**谁出发、谁不出发、何时返航/待命**”。

---

## 2. 统一任务要素（A/B共用）

### 2.1 状态（建议）
- **平台状态**：位置、速度、航向、剩余续航、当前任务状态、平台类型（艇/机 I/II）
- **区域状态**：每网格上次覆盖时间、是否超时、优先级、目标存在/上次识别时间
- **全局状态**：任务进度、历史覆盖率、违反约束次数、当前出动兵力统计

### 2.2 动作（必须包含“出发/不出发”）
对每个平台至少包含以下离散-参数化动作：
- `deploy_flag ∈ {0,1}`：是否出动（0=待命/不出发，1=出发执行）
- `task_type ∈ {巡逻, 识别, 转移, 返航, 待命}`
- `target_region_or_target_id`
- `speed_level`
- `sensor_mode`

> 这样就能直接表达“什么时候出发、什么时候不出发”，并与兵力约束绑定。

### 2.3 奖励（建议分层加权）
- `R_cover`：覆盖新鲜度/重访满足率
- `R_id`：识别周期满足度（第二阶段）
- `R_gap`：最大未覆盖时长惩罚
- `R_resource`：出动力量与能耗惩罚（激励少而精）
- `R_violation`：约束违反惩罚（超时、安全距离等）

总奖励：
`R = w1*R_cover + w2*R_id - w3*R_gap - w4*R_resource - w5*R_violation`

---

## 3. 方案A：MAPPO（CTDE）

## 3.1 核心思想
- 每个 agent 在执行时独立出动作（可共享参数）
- 训练时使用集中式 critic（看到全局），提升信用分配与稳定性
- 适合扩展到通信受限或部分失联的场景

## 3.2 训练/执行机制
- **训练**：centralized critic，actor 可共享参数（当前 demo 即该模式）
- **执行**：每个平台根据自身观测独立决策

## 3.3 任务化动作设计（含出发决策）
每个 agent 输出：
- `deploy_flag`（是否出动）
- `task_type`
- `target_id`
- `speed_level`
- `sensor_mode`

可选：把连续底层控制（推力/转向）交给低层控制器，实现“高层任务 + 低层执行”。

## 3.4 适用性
- 优点：扩展性好，易支持异构与部分去中心化执行
- 风险：若 actor 观测过于局部，会出现“全局目标-局部信息”错配

## 3.5 当前 demo 在方案A中的对应验证版本
- **已对应**：
  - `train_coverage_vmas_mappo.py`：共享 actor + 集中 critic（CTDE）
  - 连续动作控制覆盖任务
- **未覆盖关键任务要素**：
  - 暂无 `deploy_flag`（不能学“出发/不出发”）
  - 暂无返航/待命状态机
  - 暂无续航约束与资源惩罚
  - 暂无识别任务与多传感器模式
- **结论**：当前 demo 是“方案A的覆盖子集验证版”，验证了协同覆盖能力，但还不是完整任务版。

---

## 4. 方案B：集中式PPO（单中央策略）

## 4.1 核心思想
- 单个中央策略输入全局状态，一次性输出全体平台联合动作
- 训练与执行都集中化，天然“上帝视角”
- 最符合“指挥中枢统一调度”的直觉

## 4.2 训练/执行机制
- **训练**：单 actor + 单 critic（或共享 backbone 的 actor-critic）
- **执行**：中央节点直接下发每个平台动作包

## 4.3 任务化动作设计（含出发决策）
联合动作可写为：
- `A_t = {a_1, a_2, ..., a_N}`
- `a_i = (deploy_flag_i, task_type_i, target_i, speed_level_i, sensor_mode_i)`

优点是全局耦合约束（兵力上限、区域均衡、优先级）可直接在一个策略里协调。

## 4.4 适用性
- 优点：最容易体现“谁出发/谁待命/谁返航”的全局最优调度
- 风险：动作维度随 agent 数量线性到高维增长；对通信与中央节点依赖强

## 4.5 当前 demo 在方案B中的对应关系
- **当前并未实现真正方案B**（当前 actor 不是 centralized actor）
- 可视为“B 的极简近似”仅体现在：
  - 环境给了部分全局统计量（fresh_ratio/max_age/progress）
- 若要落地方案B，需改为：
  - 中央 actor 输入全局状态（含全图覆盖信息与全体平台状态）
  - 中央 actor 直接输出全体 agent 的联合动作
  - 增加出动开关、任务模式与资源约束

---

## 5. A/B 对比（任务落地视角）

| 维度 | 方案A MAPPO | 方案B 集中式PPO |
|---|---|---|
| 决策主体 | 多 agent（共享或非共享） | 单中央策略 |
| 训练范式 | CTDE | Fully centralized |
| 是否天然支持失联/弱通信 | 较好 | 较弱 |
| 全局约束协调（派兵、均衡） | 中等（依赖观测设计） | 强 |
| 维度扩展性（agent增多） | 较好 | 压力更大 |
| 与“统一指挥”贴合度 | 中 | 高 |

---

## 6. 对当前项目的建议路线

### 6.1 先做什么（低风险）
先在现有 MAPPO 上补齐任务要素，形成 **A-增强版**：
1. 在 `scenario_coverage.py` 增加 `deploy_flag`、`task_type`、`target_id` 动作通道  
2. 增加待命/返航逻辑与续航状态  
3. 奖励中加入 `R_resource`（出动与能耗惩罚）  
4. 观测中加入同伴信息与全局覆盖图压缩特征

### 6.2 再做什么（验证集中调度收益）
并行做一个 **B-最小版** 对照实验：
1. 新建中央 actor（输入全局状态）  
2. 输出全体平台联合动作（含 deploy）  
3. 与 A-增强版对比指标：重访满足率、最大 gap、平均出动力量、总航程/能耗

---

## 7. 与“当前 demo 验证版本”的明确关系（一句话）

- 当前代码是：**方案A（MAPPO）下的覆盖任务基础验证版**。  
- 还不是：包含“何时出发/何时不出发/返航补给/识别模式切换”的完整任务系统。  
- 方案B目前仅有设计意义，尚未在代码中实现中央联合动作策略。

